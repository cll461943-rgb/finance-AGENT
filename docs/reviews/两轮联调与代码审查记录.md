# 第二周两轮联调与代码审查记录

审查人：陈伦禄。结论日期：2026-07-30。

## 第一轮：存量材料接入审查

输入：第一周数据 PoC、四个接口 Schema、FinRegAgents 参考代码包、任务表。

验证：

- FinRegAgents 参考包通过 Python `compileall`；
- 其测试在收集阶段因缺少 `langchain_core`、`llama_index`、`dotenv` 中断；
- 默认数据和 QA 路径写死为 `D:\code\金融\dataset`；
- 未见共享 EvidenceRecord、BM25/Dense/RRF 主公平链路或本项目 20 题日志；
- 第一周 128 条 EvidenceRecord 样例可被 V1.0 契约读取。

审查结论：

- **Critical：** 参考包来自外部 FinRegAgents，不能作为团队原创主工程。已从仓库主目录移除并记录 Apache 2.0 来源。
- **Critical：** 四类 Baseline 没有可接入实现和正式日志，不能判定跑通。
- **Required：** 删除个人绝对路径，所有方法统一从仓库相对配置读取数据。
- **Required：** 解析、检索和回答必须共享 QueryPlan/EvidenceRecord/AnswerResponse/TraceRecord。
- **Required：** `auto_checked` 不能作为人工正确性证明。

第一轮判定：`BLOCK`，不得进入横向性能结论。

## 第二轮：统一仓库与契约 Smoke

新增：

- 严格 Pydantic 契约与公开 JSON Schema；
- 单一 `python -m regrag` 入口；
- 方法注册表和公平控制配置；
- 统一 BaselineRunResult；
- 可追溯表格取数 Smoke。

验证结果：

- `pytest -q`：13 passed；
- EvidenceStore 契约校验：128/128；
- Smoke Top-1：`DOC-032-2ddfb401b298-CELL-001`；
- 引用位置：`Sheet=各地区数据（月度）, Cell=C4`；
- 输出：全国合计 52145.77 亿元；
- Evidence Gate：`warning`，因为证据仍为 `auto_checked`；
- LLM 调用/Token/成本：0/0/0。

五轴审查：

- 正确性：契约、引用、Trace 和阻塞语义有测试；通过。
- 可读性：入口和配置集中，无本机绝对路径；通过。
- 架构：公共契约与方法适配器分离；通过。
- 安全：不提交密钥、原始赛题语料和报名材料；通过。
- 性能：仅 128 条内存 Smoke，不能外推正式延迟；条件通过。

第二轮判定：统一契约与 Smoke `APPROVE`；四类正式 Baseline 仍为 `BLOCK`。

## 待修复 Issue

1. 程仁杰提交 Hybrid 统一适配器与 20 题日志后再开性能审查。
2. 罗佳佳修复单位/Header Path/期间粒度并完成 16 份人工复核。
3. 杨怡睿冻结 CoreEval-80、RetrievalGold-40 与拒答/澄清子集。
4. 陈伦禄在上述三项通过后运行主公平横向实验。
